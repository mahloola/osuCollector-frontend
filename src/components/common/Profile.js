const Profile = () => {

    return (
        <div>
            {/* profile picture */}
            {/* username (horizonally beside it) */}
            {/* dropdown menu: My Profile, Log Out, Settings, etc */}
        </div>
    );
};

export default Profile;